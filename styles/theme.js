import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#007bff', // これは例です。プロジェクトに合わせて調整してください。
    },
    // その他の色をここに定義...
  },
  // カスタムタイポグラフィをここに定義...
});

export default theme;
